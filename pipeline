pipeline {
    agent any
    stages {
        stage ('Build'){
            steps{
                sh "make all"
            }
        }
        stage ('Test'){
            steps{
                sh "make test"
            }
        }
        stage ('Archive binary'){
            steps{
                archiveArtifacts 'out/bin/main'
            }
        }
    }
}
